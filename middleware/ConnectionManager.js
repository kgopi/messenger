module.exports = (wss) => {

    // setInterval(() => {
    //     wss.clients.forEach((ws) => {
            
    //         if (!ws.isAlive) {
    //             console.log('Connection terminated', ws);
    //             return ws.terminate();
    //         }
            
    //         ws.isAlive = false;
    //         ws.ping(null, false, true);
    //     });
    // }, 10000);
}